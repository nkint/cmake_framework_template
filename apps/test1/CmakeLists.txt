cmake_minimum_required(VERSION 2.8)
message(" === apps/test1")

include_directories(${MYPROJECT_SOURCE_DIR}/modules/foo)
link_directories(${MYPROJECT_BINARY_DIR}/modules/foo)

add_executable(test1 main.cpp)

target_link_libraries(test1 foo)
