cmake_minimum_required(VERSION 2.8)
message(" === apps/test1")

set(LIB)

message("     including the module foo")
include_directories(${CMAKE_SOURCE_DIR}/modules/foo)
link_directories(${CMAKE_SOURCE_DIR}/modules/foo)
set(LIB foo)

add_executable(test1 main.cpp)

target_link_libraries(test1 ${LIB})
